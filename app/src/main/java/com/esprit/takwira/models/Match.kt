package com.esprit.takwira.models

data class Match(var _id: String ? = null,
                var userId: String ? = null,
                var stadeId: String ? = null,
                var teamCapacity: Int ? = null

                )